# 大喜利練習アプリ 仕様書（Claude Code 用）

このファイルをそのまま Claude Code に渡してください。
同梱の `odai.json` と `extract_odai.py` も同じフォルダに置くこと。

---

## 0. これは何か

過去の大喜利ファイルから抽出したお題をランダムに出題し、自分で回答を書いたあとに
**「実際に自分が書いた答え」** と **「大喜利理論に照らしたAIの講評」** を見比べる、
ローカル実行の練習アプリ。

---

## 1. データ

### 1-1. `odai.json`（抽出済み・そのまま使える）

`extract_odai.py` で全ファイルからパース済み。実測値：

| 項目 | 数 |
|---|---|
| お題（回答が1つ以上あるもの） | 2,266 |
| 回答 | 19,037 |
| コメント付き回答 | 977 |

出典別の内訳：

| ファイル | お題数 |
|---|---|
| 茶屋_クラウド読み込ませ用_８_１５ | 2,074 |
| 大喜利天文台_2部 | 48 |
| 大喜利天文台_1部 | 33 |
| 地下一階 | 29 |
| 大喜利W杯_2024 | 22 |
| コガトナ2025 | 21 |
| 大喜利W杯2022 | 15 |
| 大喜利最強決定戦 / 発射台 | 各8 |
| 大使 / 公使 / ネタボケライフ | 各1〜4 |

スキーマ：

```json
{
  "id": 0,
  "source": "大喜利W杯_2024",
  "odai": "アメリカ人が日本旅行中、命の危険を感じた瞬間",
  "meta": "86回",
  "answers": [
    { "text": "地図の下を中腰のおばあちゃんが通った",
      "comment": "全く気配がしなかった",
      "star": true }
  ]
}
```

### 1-2. データの癖（重要・実装前に必ず読むこと）

- **`comment` フィールドは自分の書き込み。** 回答本文とは別物として、視覚的にはっきり
  区別して表示すること（薄いグレー、インデント付きなど）。
  コメントが付いている回答＝自分が手応えを感じたものなので、UI 上で目立たせる価値がある。
- **`star`（☆マーク）は品質評価ではない。** 茶屋ファイルでは時間帯の区切り記号として
  使われている。並び替えや強調の根拠に使わないこと。
- **茶屋ファイルが全体の91%を占めるが、これは短文スタイル。**
  天文台・地下一階・W杯などの長文・文脈重視のスタイルの方が本来やりたい方向。
  → **出典フィルタを必須機能とし、デフォルトは「茶屋を除く」にすること。**
- パースは完全ではない。長文の散文が回答欄に紛れているお題が数件ある（例：天文台2部の
  「シャンプー」）。異常データに落ちない実装にすること。
- 元ファイルを更新したら `python extract_odai.py <ファイルのあるフォルダ> -o odai.json`
  で再生成できる。`ハシリドコロ_大喜利理論_1 / _2` は自動で除外される。

### 1-3. 理論ファイル

`ハシリドコロ_大喜利理論_1`（約32,700字）と `_2`（約24,000字）は**お題としては使わず、
講評の評価軸としてのみ使う**。同じフォルダに置いておくこと。

理論1に含まれる主な軸（講評プロンプトで参照させる）：

- 面白さの三変数モデル：**素材 × 飛躍 × 表現**
- 条件と文脈の4象限（ハシリドコロ理論）
- 被りの理論 — 語彙の独自性は前提条件にすぎない
- パワーワードの敗北と「生活の解像度」— 勝ち語彙・負け語彙
- 語とお題の距離の定量分析（中間帯の理論）

---

## 2. 画面仕様

**1画面・縦スクロールのシングルページ。** タブで切り替えず、上から下に流れる構成にする。

```
┌─────────────────────────────────┐
│ [出典フィルタ ▼]      [残り 2,266 問]│
├─────────────────────────────────┤
│ 【お題】                          │
│ アメリカ人が日本旅行中、命の危険を感じた瞬間 │
│ 出典: 大喜利W杯_2024                │
│                                  │
│ ┌───────────────────────────┐ │
│ │ 回答を入力（複数行可）             │ │
│ └───────────────────────────┘ │
│  [ 回答する ]   [ パス → 次のお題 ]   │
├─────────────────────────────────┤  ← ここから下は回答後に出現
│ ■ ファイルにあった答え（9件）          │
│  ・ホテルで正座してみたら戻せなくなった    │
│      └ どういう仕組みだこの座り方は     │
│  ・パラシュートの説明の絵が、自分だけ半袖だった│
│  …                                │
├─────────────────────────────────┤
│ ■ AIの講評                        │
│  素材 ★★★☆☆ / 飛躍 ★★★★☆ / 表現 ★★☆☆☆│
│  被り度: 低（既出9件と語彙の重複なし）    │
│  （講評本文）                       │
│  （こう直すと良くなる、の具体案 2〜3個）   │
└─────────────────────────────────┘
```

### 操作要件

- **パス**：回答せず次のお題へ。パスしたお題は同一セッション中に再出題しない。
- **回答する**を押すまで、ファイルの答えと講評は**絶対に見えないこと**（先に見えると練習に
  ならない）。DOM に隠して置くのではなく、押した後に取得・描画する。
- 講評はAPI待ちがあるので、ファイルの答えを先に即表示し、講評は後からストリーミングまたは
  ローディング表示で追いつかせる。
- 回答後に **[ 次のお題 ]** ボタン。
- キーボード：`Ctrl/Cmd + Enter` で回答、`→` でパス。

---

## 3. 講評機能（Anthropic API）

### 3-1. 構成

- APIキーは環境変数 `ANTHROPIC_API_KEY` から読む。**コードに直書きしない。**
- モデルは `claude-sonnet-5` を既定とし、設定で `claude-opus-5` に切り替えられるようにする。
  （モデルIDと料金は https://docs.claude.com/en/docs/about-claude/models で確認すること）
- **理論ファイル2本（合計約57,000字）を毎回送るので、プロンプトキャッシュを必ず使う。**
  system ブロックに理論ファイルを入れ、`cache_control: {"type": "ephemeral"}` を付ける。
  これをやらないと1回の講評が高くつく。実装方法は
  https://docs.claude.com/en/docs/build-with-claude/prompt-caching を参照。

### 3-2. プロンプト構成

**system（キャッシュ対象）**
```
あなたはネット大喜利の評価者です。以下の理論文書を唯一の評価軸として使ってください。
一般論や当たり障りのない褒め方は禁止です。

<理論1>{ハシリドコロ_大喜利理論_1 の全文}</理論1>
<理論2>{ハシリドコロ_大喜利理論_2 の全文}</理論2>
```

**user（毎回変わる）**
```
【お題】{odai}

【既出の回答】（被り判定の材料。これらと似ていたら指摘すること）
{answers を箇条書きで全部}

【評価対象】
{ユーザーの回答}

以下のJSONのみを出力（前置き・コードフェンス禁止）:
{
  "素材": 1-5の整数,
  "飛躍": 1-5の整数,
  "表現": 1-5の整数,
  "被り度": "高" | "中" | "低",
  "被り理由": "既出のどれとどう近い/遠いか。1〜2文",
  "講評": "三変数のどこが効いていてどこが弱いかを具体的に。200字以内",
  "改善案": ["語順・語彙レベルの具体的な書き換え案", "…"]
}
```

### 3-3. 講評の質に関する指示（プロンプトに入れる）

- **「狙いすぎ」「説明的すぎ」は減点。** 自然な飛躍感と、言い切れる短さを評価する。
- **映像として絵が浮かぶ具体的な動作描写を高く評価する。** 抽象的な言い回しより
  「証拠を提示する」「一度その場を離れる」「ジェスチャーだけ残す」のような手続き的な動作。
- **パワーワードに寄りかかった回答は、理論1の「勝ち語彙・負け語彙」の節に沿って減点する。**
- 褒めるときも「なぜ効いているか」を理論の用語で説明すること。

---

## 4. 技術スタック（推奨）

抽出スクリプトが Python なので統一する：

- **バックエンド**：Python + Flask（1ファイル、`app.py`）
  - `GET  /api/odai?exclude=茶屋` → ランダムなお題（`answers` は返さない）
  - `GET  /api/odai/<id>/answers` → そのお題の答え
  - `POST /api/review` → `{odai_id, user_answer}` を受けて講評JSONを返す
- **フロント**：`templates/index.html` に素の HTML + JS 1枚。ビルド不要。
- **起動**：`python app.py` → `http://localhost:5000`
- 依存は `flask` と `anthropic` のみ。`requirements.txt` を作ること。

### 履歴保存（実装してほしい）

回答するたび `history.jsonl` に1行追記する：

```json
{"ts":"2026-08-28T14:00:00","odai_id":123,"odai":"…","answer":"…","scores":{"素材":4,"飛躍":3,"表現":5}}
```

後から自分の回答をまとめて読み返せるようにするため。閲覧UIは不要、ファイルがあればよい。

---

## 5. スコープ外（作らなくていい）

- ログイン、ユーザー管理、デプロイ、Docker
- お題の追加・編集UI（元ファイルを直して再抽出すればよい）
- 得点ランキング、連続日数などのゲーミフィケーション
- レスポンシブ対応の作り込み（デスクトップのみでよい）

---

## 6. 完成の判定

1. `python extract_odai.py` が動き `odai.json` が再生成できる
2. 起動して、茶屋を除いたお題がランダムに出る
3. パスで次のお題に進み、同じお題が再度出ない
4. 回答するまでファイルの答えが見えない
5. 回答後、同じ画面をスクロールしてファイルの答えと講評の両方が読める
6. 講評が理論ファイルの用語（素材・飛躍・表現、勝ち語彙 等）で書かれている
7. `history.jsonl` に記録が残っている
